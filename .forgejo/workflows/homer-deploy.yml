name: Deploy Homer Config
on:
  push:
    paths:
      - "k3s-ha-cluster/homer/config.yaml"

jobs:
  deploy-homer-config:
    runs-on: self-hosted

    container:
      image: git.yukselcloud.com/lab/kubectl-node:latest
      env:
        KUBERNETES_SERVICE_HOST: kubernetes.default.svc
        KUBERNETES_SERVICE_PORT: "443"
      volumes:
        - /var/run/secrets/kubernetes.io/serviceaccount:/var/run/secrets/kubernetes.io/serviceaccount
    steps:
      - uses: actions/checkout@v4

      - name: Apply Homer ConfigMap
        run: |
          # Use the ServiceAccount files directly without a full config file
          # We pass the CA cert and token via command line flags
          export KUBE_TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)

          kubectl -n homer create configmap homer-config \
            --from-file=config.yml=k3s-ha-cluster/homer/config.yaml \
            --server=https://kubernetes.default.svc \
            --certificate-authority=/var/run/secrets/kubernetes.io/serviceaccount/ca.crt \
            --token=$KUBE_TOKEN \
            -o yaml --dry-run=client | kubectl apply -f -

      - name: Rollout Restart Homer
        run: |
          export KUBE_TOKEN=$(cat /var/run/secrets/kubernetes.io/serviceaccount/token)
          kubectl -n homer rollout restart deploy homer \
            --server=https://kubernetes.default.svc \
            --certificate-authority=/var/run/secrets/kubernetes.io/serviceaccount/ca.crt \
            --token=$KUBE_TOKEN
